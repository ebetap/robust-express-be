{
  "name": "express-backend-starter",
  "version": "1.0.0",
  "description": "Starter pack for an Express.js backend with auto-generated APIs and JWT authentication.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.0.0",
    "express": "^4.17.2",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.1.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.15" // Optional: Use nodemon for auto-restarting server during development
  }
}
